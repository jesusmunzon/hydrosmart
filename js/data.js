import{DEVICES}from'./config.js';
export function generateInitialData(){const a=[],obs=[null,null,null,null,null,null,'Lectura estimada por mantenimiento de caudalímetro','Variación brusca de presión detectada','Verificación telemétrica periódica'];for(let y=2023;y>=2009;y--)for(let m=12;m>=1;m--){const date=`${y}-${String(m).padStart(2,'0')}-01`,yf=1+(y-2009)*.018,mf=1+Math.sin((m-1)*Math.PI/6)*.22;for(const d of DEVICES){let cmd=Math.round(d.baseCmd*yf*mf*(.88+Math.random()*.24)*10)/10,cmes=Math.round(cmd*new Date(y,m,0).getDate()),o=obs[Math.floor(Math.random()*obs.length)];if(Math.random()<.008){cmd=-120.5;cmes=-3615;o='Anomalía de flujo inverso detectada en telemedida'}a.push({Fecha:date,CodigoDispositivo:d.code,NombreDispositivo:d.name,Poblacion:d.muni,CodigoSenal:`SIG-${d.code}`,CMD:cmd,CMES:cmes,Observaciones:o})}}return a}